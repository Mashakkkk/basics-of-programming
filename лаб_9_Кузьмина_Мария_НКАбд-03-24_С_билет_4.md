```c
#include <stdio.h>
#include <math.h>
int main(){
    int a,b,c,d,x1,x2;
    printf("число a: ");
    scanf("%d",&a);
    printf("число b: ");
    scanf("%d",&b);
    printf("число c: ");
    scanf("%d",&c);
    d=b*b-4*a*c;
    if (d>=0){
        x1=(-b+sqrt(d))/2*a;
        x2=(-b-sqrt(d))/2*a;
        printf("1 корень: %d\n",x1);
        printf("2 корень: %d\n",x2);
    }else{
        printf("\nкорней нет");
    }
    return 0;
}
